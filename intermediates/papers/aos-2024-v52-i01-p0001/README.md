# The right complexity measure in locally private estimation: It is not the Fisher information

The registered-source review retains two complete Theorems, twelve source entries and seventeen related connections from arXiv 1806.05756v3. Existing source-audit, inventory and census bytes are preserved.

The earlier generation entrypoint was not retained. scripts/reviewed_extraction.py now holds a readable literal record recovered from the pinned, already-reviewed new-skill inventory and unfinalized census. scripts/rebuild.py writes those inputs in an empty directory and invokes the current validator/finalizer to derive the ranked census. It does not embed the ranked output or claim to perform a fresh semantic PDF review.

    python3 -B scripts/rebuild.py --output-dir [local path omitted]

All six content files reproduce exactly. The separate source-passages, interface-extraction and ambient-prerequisites files organize the same source-reviewed content and notes. See evidence/reproduction-recovery.json and evidence/rebuild-check.json for the provenance and scope. Original source ambiguities remain in the audit and ambient record.
