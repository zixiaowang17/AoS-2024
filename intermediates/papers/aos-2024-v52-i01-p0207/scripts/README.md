# Reproduction

Run python3 -B scripts/rebuild.py --output-dir [local path omitted] from this paper directory. The output directory must be empty. The script resolves the pinned PDF and replays the retained main-text extraction and current finalizer. It compares all six content artifacts without renewing source certification or modifying archived evidence.
