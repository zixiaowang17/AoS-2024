# Tensor factor model estimation by iterative projection

Source-reviewed census of the registered local arXiv:2006.02611v3 PDF, marked 18 July 2024 (58 pages). Main text ends on PDF page 24 before references. Supplementary material is excluded.

- **5 theorems:** 3.1, 3.2, 3.3, 3.4, 3.5. Theorem 3.3 continues across pages 13–14.
- **37 source entries**, 14 auxiliary passages, 35 direct theorem uses and 80 related-theorem connections.
- All six content JSON files passed structural validation and reproduced byte for byte in `[local path omitted]`.
- Independent source review checked the original statements, source formulas, paper-local graph, names and highlights. See `registered-source-review.json` and `paper-audit.json`.

The three iterative estimators remain distinct. Rate definitions are separated from surrounding risk guarantees. The computational and statistical lower bounds retain their own probability class and different hypotheses/losses. Enlarged formula crops preserve the exact scope of square roots. Thirteen source notes record unresolved notation, domains and conventions without changing the original statements.

The retained scripts are `save_inventory.py`, `review_inventory.py`, `extract_interfaces.py`, `save_ambient.py`, `finalize_paper.py`, `rebuild.py` and `save_registered_source_review.py`. Run them from this paper's `scripts/` directory or by their repository-relative paths. To reproduce content, use `python3 scripts/rebuild.py --output-dir <empty-directory>`; reproduction does not constitute a new semantic source review.

No mathlib audit or HTML generation is included in this stage.
