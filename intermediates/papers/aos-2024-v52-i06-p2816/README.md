# Noisy recovery from random linear observations: Sharp minimax rates under elliptical constraints

Source-reviewed census of the registered local arXiv:2303.12613v1 PDF, stamped 22 March 2023 (53 pages). Main text ends on page 32; appendices are excluded.

- Two theorems: 1 (General minimax upper bound) and 2 (Lower bound).
- 12 source entries, seven auxiliary passages, four direct uses and 24 related-theorem connections.
- All six content JSONs passed validation and reproduced byte for byte in `[local path omitted]`.
- Independent source review checked full statements, definitions, assumptions, dependencies, source names and highlights. See `registered-source-review.json` and `paper-audit.json`.

The census preserves conditional rather than marginal noise conditions, unrestricted measurable estimators of the observed operator and response, distinct error/constraint matrix geometry, and deterministic optimization outside operator-law expectations. Nine notes record source limitations, including covariance invertibility and radius conventions. Gaussian, ridge and infinite-dimensional application material remains separate from the general theorem requirements.

Seven retained scripts support reproduction: `save_inventory.py`, `review_inventory.py`, `extract_interfaces.py`, `save_ambient.py`, `finalize_paper.py`, `rebuild.py` and `save_registered_source_review.py`. From this directory run `python3 scripts/rebuild.py --output-dir <empty-directory>`. Rebuilding does not constitute a new semantic source review or certify theorem truth.
