# Time-uniform central limit theory and asymptotic confidence sequences

Status: complete, checked against the registered local PDF and independently validated.

- Source: arXiv:2103.06476v9, marked 14 March 2024, 69 PDF pages. Main text ends with acknowledgements on page 28 before references; evidence is clipped at y632. Appendix bodies are excluded.
- All six original main-text Theorems (2.2, 2.4, 2.8, 3.1, 3.2, 3.3) are retained. Actual printed headings control the inventory; stale outline labels do not turn Propositions or Corollaries into Theorems.
- The census contains 28 source entries, 12 auxiliary passages, 41 direct uses and 53 related-theorem connections.
- Asymptotic confidence-sequence approximation and later-start coverage remain distinct. The four coupling conditions, conditional-moment requirements, and all imported Corollary 2.6 assumptions are preserved separately.
- The observational theorem's changed propensity and regression assumptions are respected. The time-varying theorem retains full cross-fitting, uniform regression convergence and average nuisance-error conditions.
- Fourteen source notes record ambiguities, including initial zero widths, the tuning formula's domain, named-only causal assumptions, and Theorem 3.3's variance expression involving the limiting influence function. No fitted-variance substitution is invented.
- All six content JSON artifacts reproduce byte for byte in `[local path omitted]`. Seven scripts remain under `scripts/`.

See `paper-audit.json`, `registered-source-review.json`, and `evidence/manual-findings.json` for evidence and limits. Source validation does not prove the claims or repair unresolved source notation.

Rebuild in a fresh empty directory with `python3 -B scripts/rebuild.py --output-dir [local path omitted]`. Rebuilding reproduces saved extraction decisions; it does not perform a new semantic source review.
