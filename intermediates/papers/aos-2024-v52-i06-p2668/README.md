# Statistical inference for four-regime segmented regression models

Source-reviewed census of the registered local arXiv:2410.04384v1 PDF, marked 6 October 2024 (25 pages). Main text ends on PDF page 24 before the supplementary-material notice. External supplementary bodies are excluded.

- **7 theorems:** 3.1, 3.2, 3.3, 4.1, 5.1, 6.1, 6.2.
- **36 source entries**, 10 auxiliary passages, 41 direct theorem uses and 89 related-theorem connections.
- All six content JSON files passed validation and reproduced byte for byte in `[local path omitted]`.
- Independent review checked original statements, dependencies, names and highlights. See `registered-source-review.json` and `paper-audit.json`.

The empirical centroid, general centroid functional and limiting-process centroid remain separate. Twenty-one source notes record unresolved notation and apparent formula inconsistencies without rewriting the original statements. Supplementary Assumptions S2–S4, required by Theorems 6.1 and 6.2, are recorded as unresolved external prerequisites under the main-text-only scope.

The retained scripts are `save_inventory.py`, `review_inventory.py`, `extract_interfaces.py`, `save_ambient.py`, `finalize_paper.py`, `rebuild.py` and `save_registered_source_review.py`. Reproduce the six content JSON files with `python3 scripts/rebuild.py --output-dir <empty-directory>` from this paper's directory. Reproduction does not constitute a new semantic PDF review.
